
```k
requires "wasm-semantics/wasm.md"

module CONFIGURATION
    imports WASM

    configuration 
      <stylus>
        <k> .K </k>
        <callState>
          <wasm/>
        </callState>
      </stylus>

endmodule
```
